import { OverflowedDirective } from './overflowed.directive';

describe('OverflowedDirective', () => {
  it('should create an instance', () => {
    const directive = new OverflowedDirective();
    expect(directive).toBeTruthy();
  });
});
